"use client";

import { PlaceholderPage } from "@/components/PlaceholderPage";
import { caretakerNav as nav } from "@/config/dashboardNav";

export default function SettingsPage() {
  return (
    <PlaceholderPage role="Caretaker" nav={nav} title="Settings" />
  );
}
