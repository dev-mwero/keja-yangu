"use client";

import { PlaceholderPage } from "@/components/PlaceholderPage";
import { tenantNav as nav } from "@/config/dashboardNav";

export default function SettingsPage() {
  return (
    <PlaceholderPage role="Tenant" nav={nav} title="Settings" />
  );
}
