"use client";

import { PlaceholderPage } from "@/components/PlaceholderPage";
import { tenantNav as nav } from "@/config/dashboardNav";

export default function ApplicationsPage() {
  return (
    <PlaceholderPage role="Tenant" nav={nav} title="Applications" />
  );
}
