"use client";

import { PlaceholderPage } from "@/components/PlaceholderPage";
import { tenantNav as nav } from "@/config/dashboardNav";

export default function ChatPage() {
  return (
    <PlaceholderPage role="Tenant" nav={nav} title="Chat" />
  );
}
