"use client";

import { PlaceholderPage } from "@/components/PlaceholderPage";
import { ownerNav as nav } from "@/config/dashboardNav";

export default function DocumentsPage() {
  return (
    <PlaceholderPage role="Owner" nav={nav} title="Tenant Documents" />
  );
}
